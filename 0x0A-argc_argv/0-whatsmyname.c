#include <stdlib.h>
#include <stdio.h>
/**
 * main - returns name of program
 * Description: cmd argument to main
 * @argc: counts cmd arguments
 * @argv: holds arguments as string
 * Return: 0
 */
int main(int argc, char *argv[])
{
	int i = 0;

	argc = i;
	printf("%s\n", argv[argc]);
	return (0);
}
